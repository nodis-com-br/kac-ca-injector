apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: default
  annotations:
    "nodis.com.br/ca-injector": "true"
  labels:
    name: nginx
spec:
  containers:
  - name: nginx
    image: nginx
